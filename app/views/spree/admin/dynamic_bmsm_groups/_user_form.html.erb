<%= form_for user, :url => update_user_group_admin_dynamic_bmsm_group_path(group)  do |f| %>
    <%= f.hidden_field :id %>
    <%= f.collection_select :dynamic_bmsm_group_id,
                            Spree::DynamicBmsmGroup.all,
                            :id,
                            :name,
                            :prompt => true
    %>
   <%= f.submit %>
<% end %>
