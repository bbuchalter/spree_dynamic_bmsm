<%= form_for user, :url => update_user_group_admin_dynamic_bmsm_groups_path  do |f| %>
    <%= f.hidden_field :user_id,:value => user.id %>
    <%= f.collection_select :spree_dynamic_bmsm_group_id,
                            @spree_dynamic_bmsm_groups,
                            :id,
                            :name,
                            :prompt => true
    %>
   <%= f.submit %>

<% end %>
